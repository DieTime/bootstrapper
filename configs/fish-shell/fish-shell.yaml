question: "Install fish-shell?"
priority: 1
steps:
  - Add fish-shell repository:
    - sudo apt-add-repository ppa:fish-shell/release-3

  - Updating apt repositories:
    - sudo apt update

  - Installing fish-shell:
    - sudo apt install fish
